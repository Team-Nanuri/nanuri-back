# In-memory H2
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:nanuri_db;MODE=MySQL;
    username: sa

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        allow-remote-access: true
